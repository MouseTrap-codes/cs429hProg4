#include <stdlib.h>
#include <stdio.h>
#include <stdint.h>
#include <htable.h>

yo wheres the htable library, imma do the first pass of parsing
and adding addresses to labels in hashmap during class rn

check every instruction to check if the number of arguments are correct

increment program counter by 8 for data and 4 for instruction

int main(int argc, char *argv[]) {
    char buffer[1024];
    File *file = fopen ("data.txt". "r");

}